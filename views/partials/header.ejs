<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="icon" type="image/png" href="/images/logo.png">
    <link rel="apple-touch-icon" href="/images/logo.png">
    
    <!-- Prevent FOUC: Load theme immediately before any styles -->
    <script>
        // Load theme synchronously before page renders - MUST be inline and first
        (function() {
            try {
                const savedTheme = localStorage.getItem('theme') || 'light';
                const html = document.documentElement;
                html.setAttribute('data-theme', savedTheme);
                html.classList.add('theme-loaded');
                // Set background immediately to prevent white flash
                html.style.backgroundColor = savedTheme === 'dark' ? '#111827' : '#ffffff';
            } catch (e) {
                // Fallback if localStorage fails
                document.documentElement.setAttribute('data-theme', 'light');
            }
        })();
    </script>
    
    <!-- Inline critical CSS to prevent flash -->
    <style>
        /* Prevent flash of unstyled content */
        html {
            background-color: #111827; /* Default to dark to prevent white flash */
        }
        
        html[data-theme="light"] {
            background-color: #ffffff;
        }
        
        html[data-theme="dark"] {
            background-color: #111827;
        }
        
        /* Hide body until theme is loaded */
        html:not(.theme-loaded) body {
            opacity: 0;
        }
        
        html.theme-loaded body {
            opacity: 1;
            animation: fadeIn 0.2s ease-in;
        }
        
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        
        /* Smooth theme transitions - only for theme toggle, not page load */
        html.theme-transitioning * {
            transition: background-color 0.3s ease, color 0.3s ease, border-color 0.3s ease !important;
        }
    </style>
    
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/enhanced.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script src="/js/theme.js"></script>
</head>
<body>
